.. raw:: html

    <script type="text/javascript">
      var url = new URL(window.location);
      if (url.hostname.startsWith('teak-dashboard')) {
        url.hostname = url.hostname.replace('teak-dashboard', 'teak');
        url.pathname = '/projects/dashboard' + url.pathname;
        window.location.replace(url.href);
      }
    </script>
